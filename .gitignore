# Go
bin/
dist/
*.test
*.out

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/
frontend/.tmp-*.mjs

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# pi agent state
.pi/

# Local env overrides
.env
.env.local
.env.prod

# Build artifacts
agent/bin/
server/bin/
agent/agent
server/server
SHA256SUMS
SHA256SUMS.minisig
*.minisig
# W4-2 test fixture signature is committed (the other *.minisig are build artifacts).
!agent/internal/update/testdata/fixture.bin.minisig

# W3-4 release signing — the secret key never goes in git; the public key does.
keys/minisign.key
